<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Recipeez" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- Add status bar color -->
        <item name="android:statusBarColor">@color/splashScreen</item>

        <!-- Optional: Ensure icons are light on dark status bar -->
        <item name="android:windowLightStatusBar">false</item> <!-- For dark icons on light background, set this to true -->
    </style>
</resources>
